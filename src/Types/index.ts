export type TitleType = {
  image: string;
  children: string;
};

export type IsOpen = {
  open: boolean;
};

export type Music = {
  title: string;
  author: string;
  genre: string;
  rate: number;
  lyric: string;
  artwork: string;
  url: string;
  id: string;
};
